import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ensemble.angular',
  appName: 'Ensemble',
  webDir: 'dist/Ensemble',
  bundledWebRuntime: false
};

export default config;
